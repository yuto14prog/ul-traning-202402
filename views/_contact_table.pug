table 
  tr 
    th カテゴリ
    th 名前
    th Email
    th 編集
    th 削除
  each contact in contacts 
    tr 
      td=contact.category ? contact.category.name : "なし"
      td=contact.name 
      td=contact.email
      td
        a(href=`/contacts/${contact.id}/edit`) 編集
      td  
        form(action=`/contacts/${contact.id}/delete`, method="post")
          input(type="submit", value="削除")